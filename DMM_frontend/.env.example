# Product app (CEO/User) environment.
# Dev leaves this blank — Vite proxies /api to the local backend (vite.config.js).
# Production (Vercel): set VITE_API_URL to the deployed backend + /api, e.g.
# VITE_API_URL=https://dmm-backend.onrender.com/api
VITE_API_URL=
